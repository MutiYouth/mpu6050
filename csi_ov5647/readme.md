硬件是 Raspberry Pi Zero 2W, 和 配套的树莓派500W像素摄像头（CSI类型，ov5647）。

# dependencies

```
sudo apt install -y python3-picamera2

sudo apt install pipx
pipx install opencv-python
pipx install contrib-python

# pip install opencv-python==4.x.x.x -i https://pypi.tuna.tsinghua.edu.cn/simple

```